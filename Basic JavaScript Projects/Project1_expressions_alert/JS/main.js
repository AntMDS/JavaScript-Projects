var Sent1 = "This is a string", Sent2 = "This is another string"; //a list of variables

document.write(6 + 6); //printing out an expression

window.alert(Sent1 + Sent2)//makes a window alert concatenate the two variables